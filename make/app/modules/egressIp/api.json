{
  "url": "/egress-ip",
  "method": "GET",
  "response": {
    "error": {
      "message": "[{{statusCode}}] Whisper egress-ip failed: {{ifempty(body.detail, ifempty(body.title, 'request to rdap.whisper.online failed'))}}"
    },
    "output": "{{body}}"
  }
}
